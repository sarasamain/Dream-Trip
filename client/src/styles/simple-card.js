import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
  root: {
    textAlign: 'center',
    minWidth: 200,
  },
  media: {
    height: 200,
  },
});

export default useStyles;
