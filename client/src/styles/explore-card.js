import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
  root: {
    textAlign: 'center',
    maxWidth: 200,
    width: '200px',
    height: '300px',
  },
  media: {
    height: 200,
  },
});

export default useStyles;
